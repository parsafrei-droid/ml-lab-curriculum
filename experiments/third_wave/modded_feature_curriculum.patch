diff --git a/train_nano.py b/train_nano.py
index 55b596c..3c5f609 100644
--- a/train_nano.py
+++ b/train_nano.py
@@ -68,12 +68,23 @@ class Config:
     eval_subsample_features: int | None = 100
     max_train_mins: float = 2
     jackpot: float = 0.8068462330697953  # 0.8760712024651358
+    # Third Wave (run 4): feed the datasets in order of increasing feature count instead
+    # of the dump's default order. The curriculum is applied to a budget-sized subset that
+    # spans the full width distribution (see PriorDumpDataLoader) so that run 4 sees the
+    # same data as run 3 and only the order differs. The budget is the number of datasets
+    # a run can consume: steps*batch_size per epoch. Keep this equal to the run's actual
+    # epoch count so the ramp spans the full 2->20 width range within the run: a budget
+    # much larger than the run only replays the narrow end (e.g. a 400-epoch budget in a
+    # 120-epoch run never gets past n_features 7).
+    feature_curriculum: bool = False
+    curriculum_epochs: int = 120
 
 
 parser = argparse.ArgumentParser()
 parser.add_argument("--name", default="test")
 parser.add_argument("--seed", type=int, default=None)
 parser.add_argument("--epochs", type=int, default=None)
+parser.add_argument("--feature-curriculum", action="store_true")
 args = parser.parse_args()
 
 c = Config()
@@ -82,6 +93,10 @@ if args.seed is not None:
     c.seed = args.seed
 if args.epochs is not None:
     c.epochs = args.epochs
+    # keep the curriculum budget tied to the run length (see Config.curriculum_epochs)
+    c.curriculum_epochs = args.epochs
+if args.feature_curriculum:
+    c.feature_curriculum = True
 
 random.seed(c.seed)
 np.random.seed(c.seed)
@@ -89,6 +104,14 @@ torch.manual_seed(c.seed)
 torch.set_float32_matmul_precision('high')
 import torch._dynamo
 torch._dynamo.config.cache_size_limit = 128
+# dynamo compiles the first shape it sees as static and only switches a dim to dynamic
+# after observing it change. The dump's default order varies the feature width from the
+# first batches, so that promotion happens immediately; under the feature curriculum the
+# early batches all share one width, the feature dim gets baked in as static, and a
+# stride guard then fails once the width grows. Disabling the static-first assumption
+# makes the shape dynamic from the start. It is set for both runs, so it cannot bias the
+# baseline-vs-curriculum comparison.
+torch._dynamo.config.assume_static_by_default = False
 
 assert torch.cuda.is_available()
 
@@ -486,10 +509,12 @@ class Decoder(nn.Module):
 
 
 class PriorDumpDataLoader(DataLoader):
-    def __init__(self, filename, num_steps, batch_size, device):
+    def __init__(self, filename, num_steps, batch_size, device, feature_curriculum=False,
+                 curriculum_budget=None, curriculum_seed=0):
         self.filename = filename
         self.num_steps = num_steps
         self.batch_size = batch_size
+        self.feature_curriculum = feature_curriculum
         with h5py.File(self.filename, "r") as f:
             self.max_num_classes = f["max_num_classes"][0] if "max_num_classes" in f else None
             self.problem_type = f["problem_type"][()].decode("utf-8")
@@ -497,6 +522,24 @@ class PriorDumpDataLoader(DataLoader):
             self.max_rows = f["X"].shape[1]
             self.max_cols = f["X"].shape[2]
             self.sep_key = "single_eval_pos" if "single_eval_pos" in f else "train_test_split_index"
+            # Run 4: feed the datasets few-features -> many.
+            #
+            # A strict argsort over the whole 256k dump would not be an ordering change: a
+            # run only consumes num_steps*batch_size datasets per epoch and reaches the
+            # target long before the pointer leaves the narrowest datasets, so it would
+            # train on n_features 1-2 only and never see the rest of the distribution.
+            # Instead we draw a budget-sized subset spanning the full width distribution
+            # and sort *that* ascending, so run 4 sees the same data as run 3 in a
+            # few->many order and the only difference between the runs is the order.
+            if self.feature_curriculum:
+                nf = f["num_features"][:]
+                budget = min(curriculum_budget or self.datasets, self.datasets)
+                rng = np.random.default_rng(curriculum_seed)
+                subset = rng.choice(self.datasets, size=budget, replace=False)
+                self.order = subset[np.argsort(nf[subset], kind="stable")]
+                self.datasets = budget
+            else:
+                self.order = None
         self.device = device
         self.pointer = 0
 
@@ -505,10 +548,20 @@ class PriorDumpDataLoader(DataLoader):
             for _ in range(self.num_steps):
                 end = self.pointer + self.batch_size
 
-                num_features = f["num_features"][self.pointer : end].max()
-                x = torch.from_numpy(f["X"][self.pointer : end, :, :num_features])
-                y = torch.from_numpy(f["y"][self.pointer : end])
-                sep = f[self.sep_key][self.pointer : end]
+                if self.order is None:
+                    num_features = f["num_features"][self.pointer : end].max()
+                    x = torch.from_numpy(f["X"][self.pointer : end, :, :num_features])
+                    y = torch.from_numpy(f["y"][self.pointer : end])
+                    sep = f[self.sep_key][self.pointer : end]
+                else:
+                    # h5py fancy indexing needs increasing indices; the slice of a sorted
+                    # order is already increasing only by feature count, so sort the batch's
+                    # own indices before reading and keep the rows in that same order.
+                    idx = np.sort(self.order[self.pointer : end])
+                    num_features = f["num_features"][idx].max()
+                    x = torch.from_numpy(f["X"][idx, :, :num_features])
+                    y = torch.from_numpy(f["y"][idx])
+                    sep = f[self.sep_key][idx]
 
                 self.pointer += self.batch_size
                 if self.pointer >= self.datasets:
@@ -635,6 +688,9 @@ prior = PriorDumpDataLoader(
     num_steps=c.steps,
     batch_size=c.batch_size,
     device=device,
+    feature_curriculum=c.feature_curriculum,
+    curriculum_budget=c.curriculum_epochs * c.steps * c.batch_size,
+    curriculum_seed=c.seed,
 )
 c.o = prior.max_num_classes
 
